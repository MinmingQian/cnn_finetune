from __future__ import absolute_import
import numpy as np
import os


def load_data():
    x_train = []
    y_train = []
    x_test = []
    y_test = []
    return (x_train, y_train), (x_test, y_test)
